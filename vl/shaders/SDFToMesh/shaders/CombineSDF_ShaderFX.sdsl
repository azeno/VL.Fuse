shader CombineSDF_ShaderFX : Float3ToFloat, FuseCommonSDF
{
    compose Float3ToFloat A;
    compose Float3ToFloat B;

    override float Compute(float3 p)
    {
        return S(A.Compute(p), B.Compute(p));
    }
};